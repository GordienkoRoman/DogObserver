<resources>
    <!-- Base application theme. -->
    <style name="Theme.Dog_Observer" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/light_pink</item>
        <item name="android:textColor">@color/light_pink</item>
        <item name="textOutlineColor">@color/light_pink</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/gray_dark</item>
        <!-- Secondary brand color. -->

        <!-- Status bar color. -->
        <item name="android:statusBarColor" >@color/gray_dark</item>
        <!-- Customize your theme here. -->
    </style>
</resources>